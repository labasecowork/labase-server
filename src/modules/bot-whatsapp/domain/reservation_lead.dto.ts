//src/modules/bot-whatsapp/domain/reservation_lead.dto.ts
export interface ReservationLeadDto {
  summary: string; // texto resumido para el grupo
  jsonPayload: any; // objeto con todos los campos validados
}
